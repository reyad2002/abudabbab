// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // إن كنت تحتاج ترجمة الحزمة فقط:
  // transpilePackages: ['flowbite-react'],
};
export default nextConfig;
